import { FechaEntregaPipe } from './fecha-entrega.pipe';

describe('FechaEntregaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaEntregaPipe();
    expect(pipe).toBeTruthy();
  });
});
